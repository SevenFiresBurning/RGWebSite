document.querySelectorAll('.mailing-signup form').forEach(form=>{form.addEventListener('submit',async event=>{event.preventDefault();const status=form.querySelector('[role=status]');if(form.name==='mike-wilson-mailing-list'&&!form.querySelector('input[type=checkbox]:checked')){status.textContent='Please choose at least one updates list.';return;}if(['localhost','127.0.0.1'].includes(location.hostname)){status.textContent='Preview only — signup will be available after the forms are enabled and published.';return;}const button=form.querySelector('button');button.disabled=true;status.textContent='Submitting…';try{const response=await fetch(location.pathname,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(new FormData(form)).toString()});if(!response.ok)throw new Error('Submission failed');form.reset();status.textContent='Thank you! Your mailing-list request has been received.';}catch{status.textContent='We could not save your signup. Please try again later or contact us.';}finally{button.disabled=false;}});});